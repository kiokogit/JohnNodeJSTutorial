/*
var choice = 2;


switch (choice) {
    case 1:
        console.log('Proceed to Login');
        break;
    case 2:
        console.log('Reset your password');
        break;
    case 3:
        console.log('Just visiting? Work as a guest');
    default:
        console.log('We glad you came... Make one choice')
}
*/

//ternary operator

let a = 1, b = '1';
let result = (a == b) ? 'equal' : 'inequal'; //using === (strict equality) checks on data type.

console.log(result);