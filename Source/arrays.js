let a = [1,2,3,5,6,85];

let names = ['John', 'Michael'];

//to retrieve the elemets inside the array... first index is 0

d = a[1];
console.log(d);
console.log(names[0]);

//to change value, or add more elements in an array
/*
names[1] = 'Smart Joker';
a[6] = 25;

console.log(a);
console.log(names);
//add an index beyond the ones available
//length of an array

length_of_elements = a.length;

console.log(length_of_elements);
to remove element, use .push method

to remove the last element, use .pop method
*/
